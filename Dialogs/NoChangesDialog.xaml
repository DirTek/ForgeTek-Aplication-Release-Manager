<UserControl x:Class="ForgeTekApplicationReleaseManager.Dialogs.NoChangesDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource SurfaceBrush}"
             Width="420">
    <Grid Margin="28,24,28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="{DynamicResource Str.NoChanges.Title}" FontSize="18" FontWeight="Bold"
                   Foreground="{DynamicResource TextBrush}" Margin="0,0,0,16"/>

        <!-- Message -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="{DynamicResource Str.NoChanges.Msg1}"
                       FontSize="13" Foreground="{DynamicResource TextBrush}" TextWrapping="Wrap"/>
            <TextBlock Text="{DynamicResource Str.NoChanges.Msg2}"
                       FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap"
                       Margin="0,12,0,0"/>
        </StackPanel>

        <!-- OK button -->
        <Button Grid.Row="2" Content="{DynamicResource Str.Common.Ok}" Style="{StaticResource PrimaryButton}"
                Padding="32,8" HorizontalAlignment="Right" Margin="0,24,0,0"
                Click="OkButton_Click"/>
    </Grid>
</UserControl>
