<Window x:Class="ForgeTekApplicationReleaseManager.Dialogs.SignAppFilesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="560" Height="560" MinWidth="460" MinHeight="420"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow" ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Grid.Row="0"
                   FontSize="16" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextBrush}"/>

        <TextBlock x:Name="CertText" Grid.Row="1"
                   FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"
                   TextWrapping="Wrap" Margin="0,8,0,0"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,14,0,6">
            <TextBlock x:Name="CountText" FontSize="12" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
            <Button Content="Select all" Style="{StaticResource SecondaryButton}"
                    Padding="12,4" Margin="14,0,0,0" FontSize="11" Click="SelectAll_Click"/>
            <Button Content="Deselect all" Style="{StaticResource SecondaryButton}"
                    Padding="12,4" Margin="8,0,0,0" FontSize="11" Click="DeselectAll_Click"/>
        </StackPanel>

        <Border Grid.Row="3" Background="{DynamicResource SurfaceBrush}" CornerRadius="6"
                BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" Padding="10,8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="FileList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected}" Margin="0,3"
                                      Foreground="{DynamicResource TextBrush}" FontSize="12">
                                <TextBlock>
                                    <Run Text="{Binding BadgeText, Mode=OneWay}"
                                         Foreground="{DynamicResource AccentBrush}" FontWeight="Bold"/>
                                    <Run Text="{Binding RelativePath, Mode=OneWay}"/>
                                </TextBlock>
                            </CheckBox>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <TextBlock x:Name="StatusText" Grid.Row="4"
                   FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"
                   TextWrapping="Wrap" Margin="0,12,0,0"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Close" Style="{StaticResource SecondaryButton}"
                    Padding="20,9" Margin="0,0,8,0" Click="Close_Click"/>
            <Button x:Name="SignBtn" Content="Sign Selected" Style="{StaticResource PrimaryButton}"
                    Padding="20,9" Click="Sign_Click"/>
        </StackPanel>
    </Grid>
</Window>
